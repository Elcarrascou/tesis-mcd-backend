# ── Supabase ──────────────────────────────────────────────────
# Clave service_role: SECRETA, solo backend. Salta RLS y permite ESCRIBIR.
# Nunca subir a git ni exponer en el navegador.
SUPABASE_URL=https://xzedmtnouzarsslyglbe.supabase.co
SUPABASE_SERVICE_ROLE_KEY=pega_aqui_la_service_role_key

# ── CORS (opcional) ───────────────────────────────────────────
# Por defecto ya permite localhost:5173 y el dominio de Vercel.
# ALLOWED_ORIGINS=["http://localhost:5173","https://tesis-mcd-usach.vercel.app"]

DEBUG=false
